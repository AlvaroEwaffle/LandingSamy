---
interface Testimonial {
  quote: string;
  author: string;
  role: string;
}

interface SuccessMetric {
  metric: string;
  description: string;
  icon: string;
}

const successMetrics: SuccessMetric[] = [
  {
    metric: "+300K USD",
    description: "facturados por alumnos en colaboraciones",
    icon: "💰"
  },
  {
    metric: "0 a 50K",
    description: "seguidores en 6 semanas (caso @ejem_creator)",
    icon: "📈"
  },
  {
    metric: "Primer contrato",
    description: "cerrado en 6 semanas",
    icon: "🎯"
  }
];

const mediaSupport = [
  "Forbes Chile",
  "Cosmopolitan Latam", 
  "La Tercera"
];
---

<section class="py-20 bg-primary relative overflow-hidden">
  <div class="container mx-auto px-4 relative z-10" data-aos="fade-left">
    <div class="max-w-6xl mx-auto">
      <div class="text-center mb-12">
        <h2 class="text-4xl md:text-5xl font-bold text-accent mb-6">
          Historias de Éxito y Resultados Reales
        </h2>
        <div class="w-24 h-1 bg-secondary mx-auto"></div>
      </div>

      <!-- Two Column Layout -->
      <div class="grid lg:grid-cols-2 gap-12 items-stretch mb-12">
        <!-- Left Column - Video -->
        <div class="order-2 lg:order-1 flex justify-center h-full">
          <div class="bg-white/10 backdrop-blur-sm rounded-2xl border border-white/20 flex items-center h-full max-w-[360px] w-full mx-auto">
            <div class="relative w-full aspect-[9/16] rounded-xl overflow-hidden mx-auto">
              <iframe
                src="https://player.vimeo.com/video/1028650112?h=9384d48510&title=0&byline=0&portrait=0"
                class="w-full h-full"
                frameborder="0"
                allow="autoplay; fullscreen; picture-in-picture"
                allowfullscreen
                title="Influencer Factory - Whitney + Testimonios"
              ></iframe>
            </div>
          </div>
        </div>

        <!-- Right Column - Success Metrics -->
        <div class="order-1 lg:order-2 h-full flex items-stretch">
          <div class="space-y-6 w-full flex flex-col justify-center h-full">
            {successMetrics.map((metric) => (
              <div class="bg-white/10 backdrop-blur-sm rounded-2xl p-6 border border-white/20 flex items-center h-full">
                <div class="flex items-center space-x-4">
                  <div class="text-4xl">{metric.icon}</div>
                  <div>
                    <div class="text-2xl font-bold text-accent mb-1">{metric.metric}</div>
                    <p class="text-white/90">{metric.description}</p>
                  </div>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>

      <!-- Media Support -->
      <div class="text-center mb-12">
        <h3 class="text-2xl font-bold text-accent mb-6">
          Medios que nos respaldan
        </h3>
        <div class="flex flex-wrap justify-center gap-6">
          {mediaSupport.map((media) => (
            <div class="bg-white/10 backdrop-blur-sm rounded-full px-6 py-3 border border-white/20">
              <span class="text-white font-semibold">{media}</span>
            </div>
          ))}
        </div>
      </div>

      <!-- CTA -->
      <div class="text-center">
        <a 
          href="#planes" 
          class="inline-flex items-center justify-center px-8 py-4 bg-secondary text-white rounded-full font-semibold text-lg hover:bg-secondary/90 transition-colors duration-300 shadow-lg hover:shadow-xl"
        >
          Quiero ser el próximo caso de éxito
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
  }
</style> 