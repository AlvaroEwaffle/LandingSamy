---
interface TargetAudience {
  title: string;
  description: string;
  icon: string;
}

const targetAudience: TargetAudience[] = [
  {
    title: "Aspirantes a influencers y UGC creators",
    description: "Personas que quieren convertir su pasión por las redes en una carrera profesional",
    icon: "📱"
  },
  {
    title: "Emprendedores que quieren potenciar su marca personal",
    description: "Empresarios que buscan construir una presencia digital fuerte y auténtica",
    icon: "💼"
  },
  {
    title: "Cualquiera que sueñe con vivir de las redes",
    description: "Personas con el sueño de monetizar su creatividad y vivir de su contenido",
    icon: "✨"
  }
];
---

<section id="que-es" class="py-20 bg-black overflow-x-hidden w-full max-w-full">
  <div class="container mx-auto px-4">
    <!-- Title Section -->
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
        ¿Qué te espera dentro de Influencer Factory?
      </h2>
      <div class="text-6xl mb-6">🎓</div>
      <h3 class="text-2xl font-bold text-white mb-6">
        Formación integral para futuros creadores profesionales.
      </h3>
      <p class="text-xl text-white max-w-4xl mx-auto leading-relaxed mb-8">
        Desde tu mentalidad hasta tu monetización. Te acompañamos paso a paso para que construyas una marca auténtica, produzcas contenido irresistible y cobres por tu talento.
      </p>
      <div class="w-24 h-1 bg-secondary mx-auto"></div>
    </div>

    <!-- Ideal for Section -->
    <div class="mb-12">
      <h3 class="text-2xl font-bold text-white text-center mb-8">
        Ideal para…
      </h3>
      <div class="grid md:grid-cols-3 gap-8 max-w-6xl mx-auto">
        {targetAudience.map((audience) => (
          <div class="bg-accent rounded-2xl p-8 text-center hover:shadow-xl transition-all duration-300 hover:-translate-y-2">
            <div class="text-5xl mb-4">{audience.icon}</div>
            <h4 class="text-xl font-bold text-primary mb-3">{audience.title}</h4>
            <p class="text-black">{audience.description}</p>
          </div>
        ))}
      </div>
    </div>

    <!-- CTA -->
    <div class="text-center">
      <a 
        href="#programa" 
        class="inline-flex items-center justify-center px-8 py-4 bg-secondary text-white rounded-full font-semibold text-lg hover:bg-secondary/90 transition-colors duration-300 shadow-lg hover:shadow-xl"
      >
        Quiero mi lugar
      </a>
    </div>
  </div>
</section>

<style>
  .container {
    max-width: 1200px;
    margin: 0 auto;
  }
</style>

<!-- Add Remix Icons CSS -->
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/remixicon@3.5.0/fonts/remixicon.css" /> 