---
interface Props {
  title?: string;
  subtitle?: string;
}

const { 
  title = "👋 Soy Whitney Espinoza. Transformé mi pasión por las redes en una carrera; ahora te enseño a hacer lo mismo.", 
  subtitle = "Academia digital con 20 módulos, 90+ lecciones y clases nuevas cada mes para que monetices tu contenido desde cero." 
} = Astro.props;
---

<section class="relative min-h-screen flex items-center justify-center text-center bg-black overflow-x-hidden">
  
  <div class="relative z-30 max-w-7xl px-4 sm:px-6 lg:px-8">
    
     <!-- Infinite Scrolling Banner -->
     <div class="relative overflow-hidden mb-8 bg-secondary/10 border border-secondary/30">
      <div class="scrolling-banner">
        <div class="scrolling-content">
          <span class="text-sm md:text-base text-white font-semibold px-4">ASEGURA TU CUPO AHORA - 100% ONLINE - 100% VIRTUAL - 100% DIGITAL -</span>
          <span class="text-sm md:text-base text-white font-semibold px-4">ASEGURA TU CUPO AHORA - 100% ONLINE - 100% VIRTUAL - 100% DIGITAL -</span>
          <span class="text-sm md:text-base text-white font-semibold px-4">ASEGURA TU CUPO AHORA - 100% ONLINE - 100% VIRTUAL - 100% DIGITAL -</span>
          <span class="text-sm md:text-base text-white font-semibold px-4">ASEGURA TU CUPO AHORA - 100% ONLINE - 100% VIRTUAL - 100% DIGITAL -</span>
          <span class="text-sm md:text-base text-white font-semibold px-4">ASEGURA TU CUPO AHORA - 100% ONLINE - 100% VIRTUAL - 100% DIGITAL -</span>
          <span class="text-sm md:text-base text-white font-semibold px-4">ASEGURA TU CUPO AHORA - 100% ONLINE - 100% VIRTUAL - 100% DIGITAL -</span>
        </div>
      </div>
    </div>


    <!-- Logo -->
    <div class="mb-8 flex justify-center">
      <img 
        src="/assets/IF_Logo.webp" 
        alt="Influencer Factory Logo" 
        class="w-100 sm:w-100 md:w-80 lg:w-80 h-auto object-contain"
      />
    </div>

    <!-- Main Content -->
    <h1 class="text-white text-2xl md:text-3xl lg:text-4xl font-bold mb-4 drop-shadow-lg leading-tight">
      {title}
    </h1>

    <p class="text-white text-base md:text-lg max-w-4xl mx-auto mb-6 leading-relaxed">
      {subtitle}
    </p>
    
    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center mb-6">
      <a href="#planes" class="w-1/2 md:w-auto mx-auto inline-flex items-center justify-center px-6 py-3 bg-secondary text-white rounded-full font-semibold text-base hover:bg-secondary/90 transition-colors duration-300 shadow-lg hover:shadow-xl">
        ▶ Empezar ahora
      </a>
    </div>

    <!-- Social Proof -->
    <div class="text-white/90 text-base mb-4">
      ⭐ Más de 1,400 creadores formados · Actualizaciones mensuales · Acceso 24/7
    </div>
  </div>
</section> 

<style>
  .scrolling-banner {
    width: 100%;
    overflow: hidden;
    white-space: nowrap;
  }

  .scrolling-content {
    display: inline-block;
    animation: scroll-left 80s linear infinite;
    white-space: nowrap;
  }

  .scrolling-content span {
    display: inline-block;
    padding: 1rem 0;
  }

  @keyframes scroll-left {
    0% {
      transform: translateX(0%);
    }
    100% {
      transform: translateX(-200%);
    }
  }

  /* Ensure the banner spans full width */
  .scrolling-banner {
    margin-left: calc(-50vw + 50%);
    margin-right: calc(-50vw + 50%);
    width: 100vw;
  }
</style>